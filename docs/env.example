# Copy to .env in this directory (docs/) for local builds.
# GitHub Actions: set repository secret PUBLIC_VSCODE_IFRAME_URL to the same value.

# HTTPS URL of code-server (e.g. Google Cloud Run). Required for the home page iframe — no mock fallback.
PUBLIC_VSCODE_IFRAME_URL=https://your-service-xxxxx-uc.a.run.app
