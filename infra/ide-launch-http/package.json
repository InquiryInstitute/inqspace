{
  "name": "inqspace-ide-launch-http",
  "version": "1.0.0",
  "private": true,
  "description": "Public HTTP Cloud Function (Gen2): CORS + POST /ide/launch → provisioner (holds provisioner secret).",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "functions-framework --target=ideLaunch --port=8080"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "express": "^4.21.0"
  }
}
